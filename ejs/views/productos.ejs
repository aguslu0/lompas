<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lompas | Productos en EJS</title>
</head>
<body>
    <a href="../" >Cargar productos</a>

    <% if(aProducts.length) { %>
            <table>
                <tr>
                    <th>NOMBRE</th>
                    <th>PRECIO</th>
                    <th>IMAGEN</th>
                </tr>
                <% aProducts.forEach((i) => { %>
                    <tr>
                        <td><%= i.title %></td>
                        <td><%= i.price %></td>
                        <td><img height='100px' src='<%= i.thumbnail %>' alt='<% i.title %>' /></td>
                    </tr>
                <% }); %>
            </table>
    <% } else { %>
        <h2>No hay productos cargados...</h2>
    <% } %>
</body>
</html>